/*WAP to take an 1-D integer array (a[5]) and initialize it at compile time with values (200,-94,8,84,50), 
then print the base address of the array, the array elements and their memory locations.*/

#include <stdio.h>

int main(){
    int a[5]
    return 0;
}